@inherits Blazorade.Core.Components.BlazoradeComponentBase

@if (IsStaticGeneration)
{
	@((MarkupString)StaticGenerationMarkers.StaticPageStart)
}

@ChildContent

@if (IsStaticGeneration)
{
	@((MarkupString)StaticGenerationMarkers.StaticPageEnd)
}
